============
Installation
============

At the command line::

    $ easy_install django_renderblock

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_renderblock
    $ pip install django_renderblock
